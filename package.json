{
  "name": "ptero-cleanup-public",
  "version": "1.0.0",
  "description": "Public API for Pterodactyl Panel cleanup",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
